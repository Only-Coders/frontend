export type WorkExperience = {
  id?: string;
  name: string;
  position: string;
  since: string;
  until: string;
};

export type StudyExperience = {
  id?: string;
  name: string;
  degree: string;
  since: string;
  until: string;
};
